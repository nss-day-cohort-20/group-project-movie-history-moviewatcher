"use strict";

let $ = require('jquery');

//in progress

module.exports.buildMovieObj = () => {
	let movieObj = {
		// title: `$(this).data("title")`,
		// year:
		// cast:
		// userId:
		// movieId:
		// imageURL:
	};
	// console.log($(this).data("title"));
	return movieObj;
};

